name: vdoninja_sdk
description: A type-safe Dart and Flutter wrapper for the official VDO.Ninja JS SDK, supporting WebRTC peer-to-peer streaming, custom P2P data channels, and standalone WHIP/WHEP clients.
version: 0.0.2
homepage:

environment:
  sdk: ^3.11.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  web: ^1.1.1
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  integration_test:
    sdk: flutter

flutter: